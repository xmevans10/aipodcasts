name: render-episodes

# Render committed transcripts into app-ready episodes with the free Kokoro cast
# (Apache-2.0, one distinct voice per host; no key, no cloning). Co-hosted shows
# render each validated speaker turn with its own voice. Rendered files are uploaded
# as a CI artifact; the iOS app consumes episodes through the published feed.
on:
  workflow_dispatch:
    inputs:
      transcripts:
        description: Transcripts directory to render
        default: experiments/full-run/latest/transcripts
jobs:
  bundle:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      HF_HOME: ${{ github.workspace }}/.hf
      PYTHONUNBUFFERED: "1"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Require a complete reviewed batch
        run: python3 experiments/full-run/check_batch.py "$(dirname "$TRANSCRIPTS")"
        env:
          TRANSCRIPTS: ${{ inputs.transcripts || 'experiments/full-run/latest/transcripts' }}

      - name: Cache Kokoro weights
        uses: actions/cache@v4
        with:
          path: .hf
          key: kokoro-${{ runner.os }}-v1

      - name: System deps (espeak-ng, ffmpeg)
        run: sudo apt-get update && sudo apt-get install -y espeak-ng ffmpeg

      - name: Install Kokoro
        run: |
          python -m pip install --upgrade pip
          pip install kokoro soundfile numpy

      - name: Render episodes
        run: |
          python3 tools/tts/bundle_shows.py \
            --transcripts "${{ inputs.transcripts || 'experiments/full-run/latest/transcripts' }}" \
            --out rendered-episodes --require-all-shows

      - name: Upload rendered episodes
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rendered-episodes
          path: rendered-episodes/
          if-no-files-found: warn
          retention-days: 90
