# Shared context (paste first)

Paste this once at the top of every milestone prompt.

```text
Zwicky (github.com/xmevans10/aipodcasts, local /Users/xanderevans/Documents/ScienceBreak) makes
short, source-linked science audio: 3-5 minute episodes written from a paper, voiced by fictional
synthetic hosts, across 16 shows. Evidence always comes from the paper; publicity/editorial feeds
only decide what to cover.

Backend: Python standard library only. Operator CLI backend/pipeline.py (select, ingest, draft,
verify, narrate, publish); selection backend/autoselect.py; verification backend/verify.py;
dialogue backend/dialogue.py; hosts backend/hosts.py; prompts backend/podcast.py,
backend/anti_slop.py. Free Kokoro voice cast tools/tts/voice_cast.json (Apache-2.0, one distinct
voice per presenter). Weekly transcript generation runs in GitHub Actions
(.github/workflows/full-run.yml -> experiments/full-run/) and rendering/publishing in
.github/workflows/render-episodes.yml.

Delivery: tools/tts/bundle_shows.py renders solo shows with Kokoro and writes an episode payload
(story, duration, word-timed transcript, five-band envelope). tools/publish_feed.py uploads to
Cloudflare R2 (repo secrets R2_*) as v1/feed.json, v1/episodes/<16-hex-id>.json sidecars, and
v1/audio/<id>.m4a (opaque id = sha256(show|published|doi)[:16]). Co-hosted shows (Ground Truth,
Star Bros) are intentionally deferred. Live feed:
https://pub-e19f5de621fd4b4ea01c0465d0251407.r2.dev/v1/feed.json

iOS: SwiftUI in ios/Zwicky, xcodeproj generated by `cd ios && xcodegen generate` from
ios/project.yml. Library/Story/Episodes in ios/Zwicky/Models.swift; Home in HomeView.swift; player
and episode views in DetailViews.swift; read-along in TranscriptView.swift. The app defaults to the
R2 feed and fetches detailURL sidecars for read-along. TestFlight: app 6813660087, bundle
com.xmevans10.Zwicky, team 8K5ZVPCQ42, ASC key id G3X8K8ZRNJ.

Checks that must stay green:
  python3 -m pytest -q backend/tests
  ./scripts/run-ios-tests.sh
  xcodebuild -project ios/Zwicky.xcodeproj -scheme Zwicky -configuration Release \
    -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO

Non-negotiables: no secrets in the repo; never reproduce source text; preserve the exact-title,
first-author, verbatim-limitations and sign-off validators; don't add paid providers without a key
that already exists; keep everything runnable on free CI.
```
