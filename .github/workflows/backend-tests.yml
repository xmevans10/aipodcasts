name: backend-tests

# Fast, key-free gate for the pipeline: selection, evidence, verification and serving.
# No network and no credentials — every fetcher is injected, so the suite is deterministic.
on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]
    paths:
      - "backend/**"
      - "experiments/selection/**"
      - "experiments/full-run/**"
      - ".github/workflows/full-run.yml"
      - ".github/workflows/daily-episodes.yml"
      - ".github/workflows/render-episodes.yml"
      - "tools/daily_release.py"
      - "tools/publish_feed.py"
      - "tools/verify_daily_feed.py"
      - "tools/tts/**"
      - "assets/audio/kenney/**"
      - ".github/workflows/backend-tests.yml"

jobs:
  pytest:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install pytest
        run: python3 -m pip install --upgrade pip pytest

      - name: Backend tests
        run: python3 -m pytest -q backend/tests

      - name: Publicity-signal effect (deterministic, offline)
        run: python3 experiments/selection/publicity_effect.py
